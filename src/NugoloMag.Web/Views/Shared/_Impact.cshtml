@model IReadOnlyList<ImpactEstimate>
@if (Model.Count > 0)
{
    <div class="kpis">
        @foreach (var i in Model)
        {
            <div class="kpi"><b>@AgenticLabels.Range(i)</b><span>@i.Measure</span><span class="muted small">@i.Basis</span></div>
        }
    </div>
}
